// Inicializamos el componente.
const Home = () => {
    return (
        <main>
            <h2>Lista de usuarios</h2>

            <ul>Aquí irían los LI con cada usuario</ul>
        </main>
    );
};

export default Home;
